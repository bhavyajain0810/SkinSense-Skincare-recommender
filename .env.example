# Environment variables for SkinSense Streamlit app

# Base URL for the local LLM API (Docker Compose overrides this)
LLM_BASE_URL=http://localhost:8001

# Dummy API key for local development (no real secret)
LLM_API_KEY=dummy

# Logical model name passed to the LLM API
LLM_MODEL=skinsense-local

# Optional: URL of an external vision attribute extraction service
#VISION_ATTR_URL=http://localhost:9000/analyze

